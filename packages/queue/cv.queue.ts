// packages/queue/cv.queue.ts
/* import { Queue } from "bullmq";
import { connection } from "./redis";

export const cvQueue = new Queue("cv-parse", {
  connection,
  defaultJobOptions: {
    attempts: 3,
    backoff: {
      type: "exponential",
      delay: 3000,
    },
  },
}); */
